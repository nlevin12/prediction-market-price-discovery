# Public Demonstration Code

The first public code module distills two parts of the full clean-room workflow:

- deterministic SHA-256 input manifests, allowing a run to verify the files it used;
- explicit data contracts for probabilities and event–stock weekend panels.

The included CSV is synthetic and exists only to demonstrate the expected public schema. It is not research data and should not be interpreted as an empirical result.
